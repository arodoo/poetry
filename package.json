{
  "name": "poetry-repo-tools",
  "private": true,
  "version": "0.1.0",
  "description": "Repository-wide tooling for formatting and checks",
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint:frontend",
    "lint:frontend": "npm --prefix poetry-frontend run lint",
    "typecheck": "npm --prefix poetry-frontend run typecheck",
    "test:frontend": "npm --prefix poetry-frontend run test",
    "test:backend": "cd poetry-backend && mvn -q test",
    "test": "npm run test:frontend && npm run test:backend",
    "build:frontend": "npm --prefix poetry-frontend run build",
    "build:backend": "cd poetry-backend && mvn -q -DskipTests package",
    "build": "npm run build:frontend && npm run build:backend",
    "openapi:validate": "redocly lint docs/api/openapi-v1.yaml",
    "sdk:check": "node tools/ci/check-openapi-hash.mjs",
    "sdk:sync": "node tools/ci/update-openapi-hash.mjs",
    "max-lines:check": "node tools/ci/check-lines.mjs",
    "config:sync": "node tools/ci/config-sync.mjs",
    "config:validate": "node tools/ci/validate-config-sync.mjs",
    "headers:check": "node tools/ci/check-headers.mjs",
    "i18n:scan": "node tools/ci/i18n-scan.mjs",
    "ci": "npm run max-lines:check && npm run headers:check && npm run i18n:scan && npm run lint && npm run typecheck && npm run test && npm run openapi:validate && npm run sdk:check && npm run build"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,md,json,yaml,yml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@redocly/cli": "^1.28.3",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
