#!/usr/bin/env sh
# File: .husky/pre-commit
# Purpose: Minimal pre-commit checks (fast linting only).
# Heavy checks disabled for faster development workflow.
# Run 'npm run ci' manually before final push.
# All Rights Reserved. Arodi Emmanuel

echo "🚀 Pre-commit (lightweight mode)..."

# Only run fastest checks - formatting
echo "🎨 Formatting config files..."
npx lint-staged --no-stash 2>/dev/null || true

echo "✅ Pre-commit passed (lightweight mode)"
echo "💡 Run 'npm run ci' manually before final push"
