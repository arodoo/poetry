# File: common-headers.yaml
# Purpose: Common reusable parameters for headers and queries. It
# centralizes ETag, If-Match, and Idempotency-Key along with paging
# controls to ensure consistency across endpoints. All Rights Reserved.
# Arodi Emmanuel
IfMatch:
  name: If-Match
  in: header
  required: true
  schema: { type: string }
  description: Required for updates/deletes
IdempotencyKey:
  name: Idempotency-Key
  in: header
  required: false
  schema: { type: string }
  description: Idempotency key per RFC
CorrelationId:
  name: Correlation-Id
  in: header
  required: false
  schema: { type: string }
  description: Client provided correlation id echoed for tracing
Page:
  name: page
  in: query
  required: false
  schema: { type: integer, minimum: 1 }
  description: Page number starting from 1
Size:
  name: size
  in: query
  required: false
  schema: { type: integer, minimum: 1, maximum: 200 }
  description: Page size with sensible limits
