# File: tokens-collection.yaml
get:
  summary: List UI tokens and current selection
  operationId: getTokens
  tags: [themes]
  responses:
    '200':
      description: Tokens payload
      content:
        application/json:
          schema:
            $ref: '../../openapi-v1.yaml#/components/schemas/UITokens'
    '304':
      description: Not Modified (ETag matched)
    '400':
      description: Bad request
      content:
        application/json:
          schema: { $ref: '../../openapi-v1.yaml#/components/schemas/Error' }
  parameters:
    - $ref: '../components/parameters/common-headers.yaml#/IfNoneMatch'
