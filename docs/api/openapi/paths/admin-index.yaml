# File: admin-index.yaml
# Purpose: Minimal OpenAPI path file for admin endpoints. Enables the
# frontend modules checker to detect admin path presence until real
# endpoints are defined. All Rights Reserved. Arodi Emmanuel
/admin:
  get:
    summary: Admin index (placeholder)
    operationId: getAdminIndex
    tags: [admin]
    responses:
      '204': { description: No Content }
