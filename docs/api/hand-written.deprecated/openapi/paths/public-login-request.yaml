# File: public-login-request.yaml
# Purpose: Authenticate public users with username and password.
# All Rights Reserved. Arodi Emmanuel
/api/v1/public/login:
  post:
    summary: Submit login credentials
    operationId: postPublicLogin
    tags: [public]
    security: []
    parameters:
      - $ref: ../components/parameters/common-headers.yaml#/CorrelationId
      - $ref: ../components/parameters/common-headers.yaml#/IdempotencyKey
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              username: { type: string, minLength: 1 }
              password: { type: string, minLength: 1 }
            required: [username, password]
    responses:
      '200':
        description: Authentication succeeded
        content:
          application/json:
            schema:
              type: object
              properties:
                accessToken: { type: string }
                refreshToken: { type: string }
              required: [accessToken, refreshToken]
      '401': { $ref: ../components/responses/common.yaml#/Unauthorized }
      '429': { $ref: ../components/responses/common.yaml#/TooManyRequests }
      '500': { $ref: ../components/responses/common.yaml#/InternalServerError }
