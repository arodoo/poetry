# File: organization.yaml
# Purpose: Basic Organization schema for listing and management. Covers
# minimal attributes needed for v1 routes and aligns with domain docs.
# Additional fields must be additive in v1. All Rights Reserved. Arodi
# Emmanuel
title: Organization
type: object
properties:
  id: { type: string }
  name: { type: string }
  createdAt: { type: string, format: date-time }
  updatedAt: { type: string, format: date-time }
required: [id, name]
