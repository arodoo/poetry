# File: auth-refresh.yaml
# Purpose: Exchanges a valid single-use refresh token for a new JWT
# access token and next refresh token. Reuse or expired token triggers
# revocation of active chain (401). All Rights Reserved. Arodi Emmanuel
/api/v1/auth/refresh:
  post:
    summary: Refresh access token
    operationId: postAuthRefresh
    tags: [auth]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              refreshToken: { type: string }
            required: [refreshToken]
    responses:
      '200':
        description: Tokens refreshed
        content:
          application/json:
            schema: { $ref: ../components/schemas/auth-tokens.yaml }
      '400': { $ref: ../components/responses/common.yaml#/BadRequest }
      '401': { $ref: ../components/responses/common.yaml#/Unauthorized }
      '429': { $ref: ../components/responses/common.yaml#/TooManyRequests }
      '500': { $ref: ../components/responses/common.yaml#/InternalServerError }
